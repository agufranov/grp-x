# Прогресс

## Реализовано

- Два режима данных: встроенные JSON и загрузка XLSX.
- Парсинг конструкции, испытаний, исходного сигнала и кепстра.
- Линейная схема скважины с НКТ, муфтами, шаром и изолированным участком.
- Три графика: сохранённый кепстр, расчётный кепстр и исходный сигнал.
- Сворачивание графиков и корректная перерисовка скрытых SVG.
- Переключение испытаний селектом и клавишами `↑`/`↓`.
- Локальная SheetJS и статическая публикация на GitHub Pages.
- Интерактивная импульсная симуляция с синтетическим сигналом, кепстром и регуляторами шума, скорости, затухания и ширины импульса.
- Миниатюра всех объектов скважины в sticky-шапке.
- Настройки симуляции перенесены в sticky-шапку; отдельная панель и легенда скважины удалены.
- Миниатюра прилипает только при пересечении полной скважиной её sticky-положения; подписи перфораций перенесены вверх и расположены в два уровня.
- Sticky-скважина приведена к точной геометрии основной схемы без текста; лишнее пространство снизу основной схемы удалено.
- Геометрия обеих схем уплотнена: убрана половина пустоты над подписями, толстые тики симметричны и доходят до нижней границы без паддинга, sticky-копия уменьшена до 65 px и лишена тени.
- Выноска шара удалена. Направляющая курсора скрыта над шапкой и поверх обеих версий скважины; однопиксельный стык устранён перекрытием под sticky-слоем.
- Контролы шапки унифицированы: селекты имеют одинаковую геометрию, подписи стали легче и контрастнее, блок симуляции получил явный бейдж. У закреплённой шапки нет скруглений, а нижняя граница всегда видна.
- Для всех трёх кепстров добавлены независимые горизонтальные масштаб и свободное положение без границ: обычное перетаскивание растягивает и сжимает график, `Shift` + ЛКМ или средняя кнопка двигает видимый диапазон, а ПКМ и кнопка в шапке сбрасывают оба параметра.
- Основная и sticky-схема скважины получили общий интерактивный горизонтальный масштаб и свободное перемещение теми же мышиными жестами; геометрия перерисовывается без деформации маркеров и подписей.
- В шапку расчётного кепстра добавлены временное окно и мягкий полосовой фильтр с отдельным состоянием для каждого испытания, быстрым выбором участка после остановки и сбросом. Фактическая обработанная последовательность перед FFT показывается отдельным графиком.
- Равномерная сетка расчёта переведена на медианный шаг; перед кепстром удаляется линейный тренд и применяется окно Тьюки.
- Симуляция переведена с короткого импульсного сигнала на профиль остановки насоса: огибающая оценивается из измерения, содержит плато, спад, провал, восстановление и медленный хвост, а отражения приходят после момента остановки. Значения по умолчанию изменены на 0% шума и 1450 м/с.
- Горизонтальная шкала обработанного сигнала закреплена за полным временем исходной записи, поэтому изменение границ расчётного окна теперь наглядно перемещает и обрезает участок вместо повторного растягивания его на всю ширину.

## Ограничения

- Испытания без парного листа показывают сообщение об отсутствии данных.
- Исходные XLSX остаются в корне как источник генерации.
- Расчётный стандартный кепстр не обязан совпадать с сохранённым кепстром XLSX.

## История коммитов

- `379dd17` — исходный статический просмотрщик и наборы данных.
- `53c09f2` — постоянный проектный контекст и правило вести журнал/коммиты.
- `7b68bb5` — расчётный кепстр и исходный сигнал.
- `df95ef5` — сохранение ширины свёрнутых графиков.
- `7674cca` — упрощённые шапки графиков.

Следующие изменения добавлять сюда кратко, указывая хеш после создания коммита в последующем документирующем изменении либо в описании следующей задачи.
